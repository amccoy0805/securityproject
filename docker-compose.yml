version: "3.9"

services:
  gateway:
    build: .
    image: aegis-ai-gateway:latest
    container_name: aegis-gateway
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
